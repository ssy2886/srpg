# 美术资源生成指南（Phase 4）

本游戏美术策略（源自 grill-me 设计讨论的收口）：**对话立绘用日式动画厚涂风（512²+），战斗单位用 Q 版小尺寸 cel 风精灵（约 48~64px），NPC 用职业通用占位精灵换色**。所有 `portrait` / `battle_sprite` 路径已在 `characters.json` 中写死，美术资源补齐后**零改码**即可自动接入 `SupportDialog`（立绘）与 `BattleMap`/`Unit`（战斗精灵）。

本指南提供**风格锁定的提示词**，可直接喂给你的图像模型（Midjourney / NAI / SD + ControlNet 等）。关键目标：**同一角色多次生成不变脸**——靠固定种子 + 固定参考图 + 负向提示实现。

---

## 0. 通用风格锁

**正向风格词（所有图共用）：**
```
anime illustration, clean lineart, soft cel shading, fantasy JRPG character design,
vibrant but muted palette, detailed clothing, full body / portrait composition
```
**负向词（所有图共用，强烈建议常驻）：**
```
multiple characters, extra limbs, deformed hands, bad anatomy, watermark,
text, signature, 3D render, realistic photo, lowres, blurry, mutated
```
**锁定一致性的三板斧：**
1. 每个角色定一个**固定随机种子**（下表给出建议值），生成该角色所有图都复用。
2. 第一张立绘满意后，**把它作为后续图的参考图 / IP-Adapter 注入**（角色脸部一致性关键）。
3. 战斗精灵：**先用首张立绘训一个轻量 LoRA**（或 IP-Adapter），再生成 Q 版姿态，否则逐帧必变脸。

---

## 1. 角色立绘（portrait，512×768 竖构图，厚涂头像+半身）

> 文件命名：`assets/portraits/{id}.png`，与 `characters.json` 的 `portrait` 字段一一对应。

| id | 角色 | 设定 | 建议种子 | 提示词要点 |
|---|---|---|---|---|
| `lyra` | 莉拉（流亡公主·领主） | 外柔内刚、王室蓝白长裙、金色长发、剑 | 1024 | `young princess, long blonde hair, blue-and-white royal dress, gentle but determined eyes, holding a slender sword, throne room background` |
| `garrett` | 加勒特（流浪剑客） | 佣兵皮甲、披风、单手剑、沉稳 | 2048 | `mercenary swordsman, leather armor, tattered cape, short brown hair, calm expression, single-handed sword on waist, forest path` |
| `mira` | 米拉（森林贤者之徒） | 法师长袍、法杖、绿意、灵动 | 3072 | `young female mage apprentice, deep green robe, wooden staff with glowing gem, curious eyes, forest glen, floating light particles` |
| `seraphina` | 塞拉菲娜（月之咏唱者） | 银发、月白法袍、高阶法师气场 | 4096 | `silver-haired archmage, moon-white ceremonial robe, intricate staff, serene commanding aura, night sky with crescent moon` |

立绘生成举例（莉拉）：
```
prompt: anime illustration, clean lineart, soft cel shading, fantasy JRPG,
  young princess, long blonde hair, blue-and-white royal dress,
  gentle but determined eyes, holding a slender sword, throne room background,
  --seed 1024 --ar 2:3
negative: multiple characters, extra limbs, deformed hands, watermark, text, 3D
```

---

## 2. 战斗精灵（battle_sprite，Q 版 64×64 一格，精灵表）

> 文件命名：`assets/sprites/units/{id}.png`。**采用单张横排精灵表：5 帧，每帧 64×64，总宽 320×64**，帧序从左到右固定为 `idle / move / attack / hurt / dead`。
> `Unit.gd` 已用 `AnimatedSprite2D` + `SpriteFrames` 按帧自动切片，所以**替换图片时保持同一布局即可零改码**。

### 当前已实现（占位精灵）

仓库已自动生成并接入可运行的占位精灵：
- `tools/generate_sprites.py`：纯标准库生成器，随时可重跑以恢复/微调占位。
- `assets/sprites/units/{lyra,garrett,mira,seraphina}.png`：5 帧横排精灵表。
- `assets/sprites/units/_preview.png`：4 张精灵表竖排预览，便于直接查看。

占位精灵仅按职业/阵营做颜色+武器剪影区分（蓝甲剑士、红棕斧战士、紫袍法师、白袍法师），作为功能验证足够。后续你可以用下面提示词出图覆盖同名文件。

### 正式出图提示词（可直接复制）

**通用前置词（所有角色精灵表共用，不要动）：**
```
chibi RPG sprite sheet, super-deformed SD style, single horizontal row of 5 frames,
idle | move | attack | hurt | dead, each frame 64x64, pixel-aligned, clean bold outline,
flat cel shading, transparent background, no background, fantasy JRPG game asset,
consistent character across all frames, same face same outfit same weapon
```

**通用质量/比例参数（Midjourney/NAI/SD 按需调整）：**
```
--ar 5:1 --seed <角色固定种子> --style raw
Recommended: steps 25-40, CFG 7-9, sampler DPM++ 2M Karras
```

**通用负向词：**
```
multiple characters, different character per frame, split image, grid layout,
extra limbs, deformed hands, bad anatomy, watermark, text, signature,
realistic photo, 3D render, lowres, blurry, mutated, background, shadow under feet
```

**逐帧姿态描述（加到对应角色的提示词里）：**
```
idle: standing relaxed, slight breathing pose, weapon at side
move: mid-stride walking, one foot forward, slight body bob
attack: leaning forward, arm raised swinging weapon
hurt: leaning back, flinching, arms guarding face
dead: lying flat on ground, eyes closed or X eyes, KO pose
```

**逐角色完整提示词（复制即用）：**

| id | 角色 | 提示词 |
|---|---|---|
| `lyra` | 莉拉（流亡公主·领主） | `chibi RPG sprite sheet, super-deformed, young princess, long blonde hair in braid, blue and white royal dress with gold trim, slender sword, 5-frame horizontal row: idle / move / attack / hurt / dead, each frame 64x64, transparent background, flat cel shading, consistent character, fantasy JRPG game asset --seed 1024 --ar 5:1` |
| `garrett` | 加勒特（流浪剑客） | `chibi RPG sprite sheet, super-deformed, mercenary swordsman, short brown hair, leather armor, red-brown cape, one-handed sword, 5-frame horizontal row: idle / move / attack / hurt / dead, each frame 64x64, transparent background, flat cel shading, consistent character, fantasy JRPG game asset --seed 2048 --ar 5:1` |
| `mira` | 米拉（森林贤者之徒） | `chibi RPG sprite sheet, super-deformed, young mage girl, dark purple hair, purple robe with green sash, wooden staff with small gem, 5-frame horizontal row: idle / move / attack / hurt / dead, each frame 64x64, transparent background, flat cel shading, consistent character, fantasy JRPG game asset --seed 3072 --ar 5:1` |
| `seraphina` | 塞拉菲娜（月之咏唱者） | `chibi RPG sprite sheet, super-deformed, silver-haired archmage, moon-white ceremonial robe with gold accents, ornate staff with glowing orb, 5-frame horizontal row: idle / move / attack / hurt / dead, each frame 64x64, transparent background, flat cel shading, consistent character, fantasy JRPG game asset --seed 4096 --ar 5:1` |

> **出图技巧：** 同一角色所有图共用固定种子；先用立绘做 IP-Adapter / LoRA 注入，再用 ControlNet OpenPose 锁定 5 个姿态。AI 出整表容易在 `hurt/dead` 变脸，建议**每帧单独出图→透明底抠图→按 64×64 同锚点（脚底居中）拼成一行**，覆盖 `assets/sprites/units/{id}.png` 即可。

### 接入验证

替换图片后运行游戏：
- 战斗中单位会自动读取 `battle_sprite`。
- `AnimatedSprite2D` 默认循环 `idle`。
- 攻击命中时会触发 `attack`（攻方）和 `hurt`（守方，未死时）动作。

---

## 3. NPC 通用占位精灵（敌方）

敌方不按人设出图，用**职业通用模板换色**即可，彻底绕开"每人变脸"。这些占位精灵已由 `tools/generate_sprites.py` 自动生成并接入：
- `assets/sprites/units/brigand.png`（山贼：壮硕皮甲、巨斧，棕红调，`bulk` 加宽）
- `assets/sprites/units/archer.png`（弓兵：轻甲、长弓，灰绿调）

**加载规则**：`Unit.setup()` 中若角色没有 `battle_sprite` 字段（即敌方），会自动回退读取 `res://assets/sprites/units/{class_id}.png`。新增敌方职业只需在 `generate_sprites.py` 的 `CHARS` 里加一项同名配置并重跑即可；多个同职业敌人共用同一张图。若想给某个强敌单独出立绘，在 `characters.json` 加 `battle_sprite` 字段指向独立图，优先级高于职业通用图。

---

## 4. 批量生成脚本（占位模板）

把提示词整理成 CSV，用你常用的图像 API 批量出图。示例（Python 调本地 SD 的占位，按需替换 endpoint）：

```python
# gen_art.py —— 占位模板，需替换为你自己的图像生成 endpoint / key
import csv, requests, os

OUT = "srpg/assets"
PROMPTS = "art_prompts.csv"   # 列: path, prompt, negative, seed

os.makedirs(f"{OUT}/portraits", exist_ok=True)
os.makedirs(f"{OUT}/sprites/units", exist_ok=True)

with open(PROMPTS, encoding="utf-8") as f:
    for row in csv.DictReader(f):
        # resp = requests.post(ENDPOINT, json={...})  # 你的调用
        # open(row["path"], "wb").write(resp.content)
        print("生成:", row["path"], "| seed", row["seed"])
```

`art_prompts.csv` 字段：`path, prompt, negative, seed`，逐行对应第 1/2/3 节各图。

---

## 5. 接入与验证（零改码）

1. 把生成好的图按第 1/2/3 节命名放入 `assets/portraits/` 与 `assets/sprites/units/`。
2. `characters.json` 的 `portrait` / `battle_sprite` 已指向这些路径，**无需改动**。
3. 运行游戏：
   - 营地（大地图按 `C`）→ 角色标签：立绘框显示对应角色头像。
   - 战斗中：`Unit` 读取 `battle_sprite` 并通过 `AnimatedSprite2D` 切片播放 `idle`/`move`/`attack`/`hurt`/`dead`。
   - 支持对话：解锁后弹出立绘演出。

---

## 6. 想让我直接出图？

若你希望我直接用内置图像模型批量生成立绘（4 张）并把文件落到 `assets/portraits/`，告诉我即可——我会按第 1 节提示词 + 固定种子生成，生成后你只需在 `SupportDialog`/营地中查看效果。战斗 Q 版精灵表因涉及逐帧+抠图，建议先出立绘、精灵表留作后续（或我先用单张 idle 帧验证管线）。
