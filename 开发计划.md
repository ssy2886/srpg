# 战旗游戏（剑与魔法大陆）· 垂直切片开发计划

> 引擎：Godot 4（GDScript）｜平台：PC 单机｜规模：1 章 / ~6 图 / 8~12 角色
> 本文档基于已锁定的 6 大设计分支。所有数值/机制的最终解释权以 `data/` 下的 JSON 为准。

---

## 0. 工程骨架（Phase 0）

**目标**：Godot 工程能跑起一个空战斗场景 + 数据加载层。

- [ ] 新建 Godot 4 工程，固定分辨率（如 960×540，可缩放窗口）。
- [ ] 建立目录：`data/`（JSON）、`src/`（脚本）、`scenes/`（战斗/大地图/对话）、`assets/`（立绘/精灵，按 `assets/portraits/`、`assets/sprites/units/`、`assets/sprites/classes/`、`assets/maps/` 分）。
- [ ] `DataManager`（Autoload）：启动时加载 `classes.json / characters.json / skills.json / weapons.json` 进内存字典，提供 `get_class(id)` 等查询。
- [ ] 约定**动画命名规范**（为 a→b 美术升级留口）：每个单位精灵表必须含 `idle / move / attack / hurt / dead` 五段，按帧名而非像素引用；画布尺寸与脚底锚点全局统一（先定 64×64）。
- [ ] 约定**数据驱动原则**：职业、成长率、技能、武器全部走 JSON，逻辑只认字段不认具体角色。

**验收**：启动后 `DataManager` 能打印出已加载的角色/职业数量；空 TileMap 场景可运行。

---

## 1. 战斗核心（Phase 1）

**目标**：玩家阶段/敌方阶段分段制网格战斗，含命中/伤害/暴击（True Hit）、武器三角、地形。

- [ ] 网格地图：TileMapLayer + 单位节点（坐标=格），移动范围用 BFS 算（受地形代价影响）。
- [ ] 回合循环：玩家阶段（可行动单位高亮）→ 敌方阶段（AI 逐个单位执行）。
- [ ] 战斗结算：
  - 命中 `Hit = 技×2 + 运/2 + 武器命中 + 支援 + 地形`；闪避 `Avo = 速×2 + 运 + 地形 + 支援`；**True Hit = 两次 `rand(0,99)` 取均值再判**（<命中则中）。
  - 伤害 `= 攻击力 − 防御`（魔法用魔/魔防）；暴击 `= 技/2 + 武器暴击`，暴击伤害 ×1.5（或 ×2，写在 `DataManager` 常量）。
  - 武器三角：克制方 `命中+15、伤害+1`（物理 剑>斧>枪>剑；魔法 理>光>暗>理）。
- [ ] 地形表：平原/森林/山地/要塞等，各给 `防御+/闪避+/移动代价`。
- [ ] 战斗结果浮字（伤害/MISS/暴击）、击杀判定（HP≤0）。

**验收**：手动摆 2 个测试单位能走、能打、能 MISS/暴击，三角克制数值正确，True Hit 手感稳。

---

## 2. 职业与成长（Phase 2）

**目标**：三阶分支转职、升级成长率、武器等级、双层技能系统。

- [ ] 角色升级：LV+1 时按 `成长率%` 逐属性掷骰（含角色成长修正）+ 体格可成长；移动为职业常量不成长。
- [ ] 转职：满 10 级且持有转职道具 → 选 `promotes_to` 中之一 → 重置 LV=1、继承属性、套用新职业 base/growth、解锁职业技能。
- [ ] 武器等级：每类武器 E→D→C→B→A→S，战斗中用该类武器攒武器 EXP；等级影响命中/伤害加成（常量在 `DataManager`）。
- [ ] 技能系统（数据驱动 `trigger` 字段）：
  - `passive` 常驻（如邻格友军受伤−1）
  - `active` 主动指令（单位菜单触发，`cooldown` 回合）
  - `conditional` 条件自动（如 HP<30% 闪避+20）
- [ ] 体格/救援：救援者 `con ≥ 被救者 con`（含装备重量），搬运影响移动。

**验收**：一个角色能从基础职转到两个进阶职之一并重置；升级数值随成长率波动合理；技能三类都能在战斗中触发。

---

## 3. 角色与叙事（Phase 3）

**目标**：角色数据、支持对话系统、大地图探索层。

- [ ] 角色数据：基础属性、个人技能、背景故事文本、立绘/精灵路径、所属基础职。
- [ ] 支持系统：任意两角色同场出战累计回合解锁 `C→B→A（→S）`；同级两人在场给数值加成；对话用立绘+对话框演出（支持脚本：说话人/立绘位置/文本）。
- [ ] 大地图层（圣魔之光石式）：章节间回到大地图，可移动探索支线地点。
  - 支线显露条件：`unlock_by_character`（拥有某角色）或 `unlock_by_story_flag`（主线进度）。
  - 支线奖励：`reward_item`（强力道具）或 `reward_character`（强力英雄），不影响主线。
- [ ] 关键抉择点：1~2 个节点放分支选项（影响后续可用角色/flag），垂直切片先只做单线。

**验收**：两人并肩打几关后能触发一段 C 级支持对话并拿到战场加成；大地图上能看到并进入一个支线地点拿到奖励。

---

## 4. 美术接入（Phase 4）

**目标**：按"立绘 anime + 战斗 Q 版小精灵"产出并接好路径（a 方案，架构留 b 口）。

- [ ] 立绘：每个角色 1 张 anime 厚涂（≥512²），固定风格提示词 + 固定种子 + 参考图锁一致性。
- [ ] 战斗精灵：每角色 `idle/move/attack/hurt/dead` 关键帧，Q 版 64×64 cel 风，统一锚点；NPC 用职业通用模板。
- [ ] 接入：`DataManager` 的角色 `portrait`/`battle_sprite` 路径指向生成的图；动画按命名段播放。
- [ ] **a→b 升级预留**：换 b 时只替换 `assets/` 图片（同尺寸同锚点同段名），逻辑零改动。

**验收**：战斗单位显示对应 Q 版精灵并能播五段动画；对话显示立绘。

---

## 4.5 抉择点系统（数据驱动分支 · Phase 5 追加）

**目标**：在关键剧情处提供可影响后续进程的分支选择，全部数据驱动、零改码扩展。

- [ ] `data/decisions.json`：每个抉择点含 `title/prompt/options`；`option.effects` 支持 `set_flags`/`clear_flags`/`grant_char`/`remove_char`/`grant_item`/`result_text`。
- [ ] 触发方式两种：
  - **世界节点触发**：`world.json` 的节点加 `"decision": "<id>"`，进入该节点且未决定时弹窗（纯剧情节点可无 `map_id`）。
  - **战后触发**：地图 JSON 顶层加 `"decision": "<id>"`，胜利后由大地图弹出（仿 `pending_support`）。
- [ ] `Campaign` 记录 `decisions_made`（`once` 不重复）与 `pending_decision`（战后弹窗中转）。
- [ ] `DecisionDialog`（全屏暗化 + 选项按钮）：选择即应用 `effects` 并写入 `decisions_made`，`on_close` 回调刷新大地图。
- [ ] 分支显露：利用 `world.json` 的 `requires_flag` / `forbid_flag` 实现互斥分支（如霜峰/河谷二选一）。

**已落地示例**：
- 抉择点 `ch1_crossroads`（世界节点 `ch1_crossroads`）：霜峰捷径 ↔ 河谷大道，互斥显露 `ch1_frost`/`ch1_valley` 两张分支地图。
- 抉择点 `ch1_captive`（地图 `ch1_01` 战后）：收编入队（招募 `rook`）/ 放走 / 处决，影响 `recruited_captive` 等 flag。

**验收**：通关 ch1_01 后弹出俘虏处置；大地图上进入「三岔口」弹分支抉择，选后仅对应分支地图显露。

---

## 5. 垂直切片内容（Phase 5）

**目标**：填满 1 章 6 图 8~12 角色，跑通全链路并平衡。

- [ ] 8~12 名角色（含领主主角 + 群像），各自背景/个人技能/支持对象。
- [ ] 6 张地图（含 1 教学关、若干支线地点），地形与敌方配置。
- [ ] 主线 1 章剧情文本 + 至少 1 个抉择点 + 1 个支线。
- [ ] 数值平衡 pass：成长率/武器/敌人强度大致可玩。

**验收**：从标题→大地图→6 图→结局能连续通关；支持对话与支线可达。

---

## 6. 打磨（Phase 6）

### 6.1 存档系统 + 永久死亡开关（已实现 ✅）

- [x] **存档单例** `SaveManager`（Autoload）：序列化到 `user://save.json`，提供 `save/load/has_save/delete_save`。
- [x] **战役状态中枢** `Campaign`：新增 `permadeath` 开关与 `roster`（角色进度字典）；`serialize()/deserialize()` 覆盖通关/拥有角色/道具/flag/抉择/支持进度。
- [x] **跨战养成持久化**：`Unit.setup` 读取 `Campaign.roster` 还原 lvl/exp/stats/武器熟练度/职业/道具；胜利后由 `BattleController._apply_battle_result` 把存活者进度写回 `roster`。
- [x] **永久死亡规则**：`permadeath=true` 时，本场阵亡的我方单位在胜利后从 `roster` 与 `owned_chars` 永久抹除；`false`（默认·重伤撤退）则保留，下场满状态回归。
- [x] **战败还原**：开战前对 `Campaign` 拍快照，战败（`_end_battle(false)`）`deserialize` 还原到进战前，可重打，养成/招募不丢失。
- [x] **标题/设置场景** `TitleScreen`：新游戏 / 继续（有档才亮）/ 永久死亡开关 / 退出；新游戏清旧档并套用开关选择。
- [x] **落盘时机**：胜利后、`WorldMap` 弹支持/抉择后、`Esc` 回标题前均 `SaveManager.save`。
- [x] **工程配置** `project.godot`：主场景设为 `TitleScreen`，注册 `DataManager/Campaign/SaveManager` 为 Autoload（此前缺主场景与 Autoload，工程无法在 Godot 中直接运行）。

- [x] **营地属性面板**：`Camp._show_detail` 读 `Campaign.roster` 实时进度（Lv/经验/HP/力量/魔法/技巧/速度/幸运/防御/魔防/体格/移动 + 当前武器等级），而非仅职业基础。
- [x] **技能说明 UI**：营地详情列出每个个人技/职业技能的【标签】名称 + 描述 + 类型/冷却（取自 `skills.json.description`）。
- [x] **战斗内血条**：`Unit` 头顶加 `UnitHud`（新节点，`src/UnitHud.gd`），绘制在精灵之上，显示 HP 条（我方绿/敌方红）+ 名称/Lv，护盾有蓝色提示条；HP 变化时由 `Unit.refresh_label` 触发刷新。
- [x] **战斗内属性面板**：`BattleController` 右上 `Panel` 显示选中单位十维属性/HP/武器/武器等级/技能；选中并悬停攻击范围内敌人时改显敌人预读；仅悬停也显示该单位。
- [x] **战斗预测窗口**：选中我方 + 悬停攻击范围内敌人时，右下 `Panel` 显示武器相克（有利▲/不利▼/无）、双方命中%/伤害/暴击%、反击判定与预计残血。`Combat.gd` 抽 `_calc` 供 `resolve`（掷骰）与新增 `forecast`（确定性，不掷骰）共用，避免预测与实战公式漂移；`BattleController._attack_params` 统一收集技能/支援/三角/熟练度/地形加成，实际结算与预测共用。
- [x] **相克图标化**：新增 `src/WeaponTriIcon.gd`（`Control` 子类，纯 `_draw` 绘制无图片资源），在预测窗口「武器相克：」行渲染彩色图标——上三角(绿/有利)、下三角(红/不利)、菱形(灰/无)；结论文字同步着色。预测面板由单 `Label` 改为 `VBoxContainer` 多行布局，动态紧贴属性面板下方。
- [ ] 导出 PC 单机包（Windows）。

**验收**：标题→新游戏（可切 permadeath）→大地图→战斗→胜利后角色升级/转职跨战保留；permadeath 下阵亡者永久消失；`Esc`/关游戏后再「继续」能恢复进度；战败可重打。

---

## 技术约定速查

| 项 | 约定 |
|---|---|
| 语言 | GDScript |
| 数据 | `data/*.json`，`DataManager`(Autoload) 加载 |
| 单位动画 | `AnimatedSprite` 按 `idle/move/attack/hurt/dead` 段名播 |
| 画布/锚点 | 战斗精灵 64×64，脚底锚点统一 |
| 三角加成 | 克制：命中+15、伤害+1 |
| True Hit | 两次 `rand(0,99)` 取均值判命中 |
| 转职 | 10 级 + 道具 → 重置 LV=1 → 选 `promotes_to` 之一 |
| 属性 | HP/力/魔/技/速/运/防/魔防/体格/移动（移动=职业常量） |
